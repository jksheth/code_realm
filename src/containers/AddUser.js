import React from "react"
import FormPanel from "./FormPanel"

const FormPanel = () => {
    return(
        <React.Fragment>
            <h2>Add details</h2>
            <FormPanel/>
        </React.Fragment>>
    );
}